---
title: Badge
description: 카드 위에 얹는 표식 — solid · soft · dot.
---

<Preview name="badge-basic" />

`solid` 는 ink 필 위 mono 라벨이고, `soft` 는 무테 메타입니다. `dot` 은 선행 점을 켭니다 —
숫자·비율처럼 앞에 표지가 필요한 라벨용입니다.

## Props

<Props of="Badge" />
