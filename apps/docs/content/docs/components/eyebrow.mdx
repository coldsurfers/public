---
title: Eyebrow
description: mono uppercase 라벨.
---

<Preview name="eyebrow-basic" />

카테고리와 PICK 은 `accent`, LIVE 와 ISSUE 는 `muted` 가 기본 짝입니다.

크기는 `editorialType.eyebrow` 슬롯에 대응합니다 — `xs`(10px) · `sm`(11px) · `md`(12px).
tracking 은 크기마다 다릅니다: 0.18em · 0.20em · 0.26em.

## Props

<Props of="Eyebrow" />
