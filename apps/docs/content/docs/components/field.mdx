---
title: Field
description: 테두리 입력 셸.
---

<Preview name="field-basic" />

`trailing` 은 우측 슬롯입니다 — ↵ 힌트나 작은 버튼이 들어갑니다.

`tone="dark"` 는 다크 밴드(`color.strong`) 위에 앉히는 변형입니다. 기본은 라이트 표면입니다.

`ref` 는 내부 실제 `<input>` 을 그대로 노출합니다 — react-hook-form 의 `defaultValues` 호환입니다.

## Props

<Props of="Field" />
