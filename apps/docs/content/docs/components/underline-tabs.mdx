---
title: UnderlineTabs
description: 밑줄 탭 줄.
---

<Preview name="underline-tabs-basic" />

아래 괘선만 긋는다 — `gap` · 정렬 · 바깥 여백은 소비처가 준다. 실측 4벌이 전부 다른 값이었다.

## 선택 신호를 엘리먼트가 정한다

**링크면 `aria-current="page"`, 버튼이면 `aria-pressed`.** 라우팅 링크에 `role="tab"` 을
붙이는 건 오히려 틀렸다 — WAI-ARIA 탭 패턴은 화살표 키 이동을 요구하는데 링크 네비엔 그게 없다.
역할만 있고 동작이 없는 쪽이 무표시보다 나쁘다.

`asChild` 일 때 `aria-current` 를 **기본으로 넣어준다**. 소비처가 명시하면 그게 이긴다.
이 기본값이 이 컴포넌트가 고치는 실제 결함이다 — 이전엔 선택 상태가 스크린리더에 전혀 안
읽히고 색만 갈렸다.

```tsx
<UnderlineTabs className={sprinkles({ gap: '7' })}>
  {items.map((it) => (
    <UnderlineTab key={it.key} active={tab === it.key} asChild>
      <Link to={it.to}>{it.label}</Link>
    </UnderlineTab>
  ))}
</UnderlineTabs>
```

화살표 키 이동(roving tabindex)은 범위 밖이다 — `role="tab"` 을 쓰는 자리가 실측 1벌로
줄어 요구가 거의 사라졌고, 넣으면 축이 스타일 밖으로 나간다.

## Props

<Props of="UnderlineTabs" name="UnderlineTabProps" file="UnderlineTabs.tsx" />
