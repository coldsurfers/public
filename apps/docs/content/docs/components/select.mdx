---
title: Select
description: 단일 선택 드롭다운.
---

<Preview name="select-basic" />

트리거(현재값 + caret) + 팝오버 메뉴다. controlled 이고 라우터·데이터에 의존하지 않는다 —
소비처가 `value` / `onChange` 를 들고, 옵션 라벨만 넘긴다.

팝오버 뼈대(바깥클릭 · Escape · 위치잡기 · maxHeight)는 [Popover](/docs/components/popover) 가 소유한다.

## Props

<Props of="Select" />

### SelectOption

<Props of="Select" name="SelectOption" />
