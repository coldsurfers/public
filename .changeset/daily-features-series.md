---
'@coldsurfers/data-models': minor
---

`/daily` 시리즈 축에 `features` 를 연다. 형제 셋(`new-shows`·`weekend`·`popular`)은 상류가
달력이나 조회수라 자동으로 서는데, 명절·연휴처럼 그때만 서는 편은 붙일 자리가 없었다.
키만 늘리고 라벨·리드는 `@paul-rockstar/daily` 의 `DAILY_SERIES` 가 든다.
