/**
 * `@coldsurfers/design-system` — 정본 배럴.
 *
 * 여기가 갖는 건 **계약과 순서**다: 어떤 이름을 쓸 수 있는가(`vars`)와
 * 겹쳤을 때 누가 이기는가(`@layer`). 값 자체는 `./tokens` 서브패스.
 *
 * 아직 비어 있다 — 뼈대만 세운 상태다.
 * 채우는 건 coldsurfers/paul-rockstar#220 의 P2(병합).
 *
 *   packages/design-system/src/{contract,theme,reset,layers,sprinkles}.css.ts
 *   packages/design-system/src/{layers,media,component-layer}.ts
 *     → 여기와 src/css/
 */
export {}
