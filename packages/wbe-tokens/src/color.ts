/**
 * WHITE BLIND EYE 팔레트 — **스킴은 다크 하나뿐이다.**
 * 커버 아트가 전부 흑백 디더라 색을 뒤집는 축이 없다. 라이트 스킴은 만들지 않는다.
 *
 * 대비(bg #0a0a0a 기준): fg 19.6:1 · muted 6.2:1 · line 1.4:1
 * **`line` 으로 읽는 글자를 찍지 않는다** — 구분선·빈 슬롯 테두리·비활성 표식 전용이다.
 */
export const color = {
  /** 지면 바탕. 커버의 검정(#000)보다 한 단 밝아 커버가 면으로 떠오른다. */
  bg: '#0a0a0a',
  /** 본문·제목·장부 값. */
  fg: '#ffffff',
  /** 라벨·캡션·메타. 읽는 글자의 하한선. */
  muted: '#8c8c8c',
  /** 구분선·빈 슬롯·비활성. 글자에는 쓰지 않는다. */
  line: '#292929',
} as const

export type ColorToken = keyof typeof color
