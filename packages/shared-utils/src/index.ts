/**
 * 루트 배럴 — **환경 중립 유틸만** 올린다.
 *
 * DOM·React·Next 를 무는 유틸은 여기 올리지 않는다. 배럴 한 줄이 그 의존을 재노출하면
 * DOM lib 없이 도는 소비자(Fastify 서버 등)의 타입체크가 깨진다 — 그 유틸을 실제로 쓰는지와
 * 무관하게. 그런 건 진입점을 갈라 서브패스로만 연다(`vite.config.ts` 참조).
 */
export * from './date'
